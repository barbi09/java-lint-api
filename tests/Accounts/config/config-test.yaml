config:
  token-manager:
    tokensInfo:
      - id: CTSRESTEXECUTOR
        generatorType: CTSRESTEXECUTOR
        host: cts-rest-executor
        endpoint: security
        clientId: ${credentials.token-manager.tokensInfo.CTSRESTEXECUTOR.clientId}
        clientSecret: ${credentials.token-manager.tokensInfo.CTSRESTEXECUTOR.clientSecret}
        ttl: 1000

